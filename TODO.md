# PhotoMinder 待办任务清单（TODO）

> **最后更新**：2026-09-13（F3 完成、版本 v0.6.0；**N8 来源持久化与恢复已完成**；批次 C 仍余 K16 / K20 / K26；v0.6.1 余 N9 / N10）
> **负责人**：now&here（个人项目，下列所有条目默认由维护者本人负责）
> **定位**：个人自用的本地照片 / 视频整理工具，macOS 为主。
> **优先原则**：用起来舒服、稳定、不丢数据 ＞ 功能数量；够用即止，非必要不引入复杂度（云端、多平台、大重构一律靠后）。
> **编号约定**：**N = 当前待办任务** · **F = 功能改进点** · **K = 已知问题** · **X = 明确延后（非必要不启动）**。
> **优先级定义**：**高** = 影响稳定性或日常使用 · **中** = 结果不准或体验明显受损 · **低** = 边界场景或锦上添花。
> **预计完成时间**：以版本号为目标节点（个人项目不排具体日期），随开发节奏滚动调整。

---

## 一、待办任务总览

| 编号 | 任务 | 优先级 | 预计完成时间 | 负责人 | 状态 |
| --- | --- | --- | --- | --- | --- |
| N5 | 稳定性加固（本地场景） | 高 | v0.5.1 | 维护者 | ✅ 已完成（2026-09-12） |
| N7 | 封面落地（或移除半成品） | 低 | v0.5.1 | 维护者 | ✅ 已完成（方案二：移除功能） |
| N8 | 库来源持久化与恢复 | 高 | v0.6.1 | 维护者 | ✅ 已完成（2026-09-13） |
| N9 | 操作日志与单次撤销（F2 降级版） | 高 | v0.6.1 | 维护者 | 待开始 |
| N10 | 配置备份与路径重连 | 高 | v0.6.1 | 维护者 | 待开始 |
| F1 | 手动相册 | 中 | v0.6（按需） | 维护者 | 待开始 |
| F2 | 撤销安全网 | 中 | v0.6（降级为 N9） | 维护者 | 待开始 |
| F3 | 浏览形态补充（地图浏览 / 幻灯片强化） | 低 | v0.6 | 维护者 | ✅ 已完成（2026-09-12） |
| F4 | 元数据写入（EXIF 写回） | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F5 | AI 增强（标签成组 / ~~批量分析~~ 并入 F7） | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F6 | 实况照片合并 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F7 | AI 结果消费出口（标签云 / 未分析 / 批量队列） | 中 | v0.6.2 | 维护者 | 待开始 |
| F8 | 重复检测闭环（忽略组 / 仅选中 / 清理报告） | 中 | v0.6.2 | 维护者 | 待开始 |
| F9 | 未整理收件箱 | 中 | v0.6.2 | 维护者 | 待开始 |
| F10 | 归档备份（保结构增量复制） | 中 | v0.6.2 | 维护者 | 待开始 |
| F11 | 文件夹树浏览 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F12 | 地点 → 事件（自动聚合「旅行」） | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F13 | 地点改名与地图批量操作 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F14 | 网格状态可读性 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F15 | 缩略图空闲预热 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| F16 | 存储占用分析 | 低 | v0.6+（按需） | 维护者 | 待开始 |
| K1–K6 | 已知问题 · 批次 A（稳定性与容错，与 N5 同批） | 中 | v0.5.1 | 维护者 | 进行中（K1 / K2 已随 N5 完成） |
| K7–K13 | 已知问题 · 批次 B（数据与结果正确性） | 中 | v0.5.2 | 维护者 | 待开始 |
| K14–K26 | 已知问题 · 批次 C（交互与体验） | 低 | v0.5.x 随批消化 | 维护者 | 进行中（K14 / K15 / K17 / K18 / K19 / K21 / K22 / K23 / K24 / K25 已完成） |
| K27–K30 | 已知问题 · 批次 D（视觉与身份） | 低 | v0.6.x 随批消化 | 维护者 | 待开始 |

> **推进方式**：N5 + 批次 A → 批次 B → 批次 C → **N8 / N9 / N10（v0.6.1：敢用 + 记住）** → **F7–F10（v0.6.2：补闭环）** → F1–F16 与批次 D 按需。每完成一项即验证「重启后是否仍顺手 / 是否会丢数据 / 视频是否别扭」，并回填本清单状态。

---

## 二、当前待办任务（N）

### N5 · 稳定性加固（本地场景）

- **具体描述**：文件操作的异常兜底与恢复——
  - 外部已删除 / 移动的文件在操作时给出明确提示并自动从列表剔除；
  - 对目录做轻量监听（`fs.watch`）以感知外部增删；
  - IPC 失败统一走 Toast；
  - 导入中途失败可续 / 可重试，不留半状态；
  - 重命名 / 删除并发保护（已有提交锁，补齐目录场景）。
- **优先级**：高
- **预计完成时间**：v0.5.1
- **负责人**：维护者
- **验收标准**：模拟文件被外部删除 / 移动 / 权限变化，应用不崩、状态自洽、提示清晰。
- **完成说明（2026-09-12）**：已全部落地（含同源的 K1 / K2，见批次 A 各自的完成说明）——
  - **目录监听**：主进程 `fs.watch`（macOS 递归）+ 按扩展名过滤 + 500ms 防抖 / 3s 强制聚合，新增 `watch-directory` / `unwatch-directory` IPC 与 `directory-changed` / `directory-watch-error` 事件；渲染层 `loadDirectory` 成功后自动监听、清空列表与卸载时停止，目录被外部删除 / 卷卸载时提示并停止监听；
  - **外部变动应用**（`applyWatcherEvent`）：消失项按前缀匹配走统一剔除管线（塌陷动画、收藏 / 标签保留）、新增文件 stat 后自动入库、拖入目录走静默扫描，汇总为一条「外部变动：新增 X 项 / 移除 Y 项」；
  - **回环防护双保险**：操作期间（提交锁内）事件暂存，锁释放后合并应用（`flushPendingWatcherEvents`）；操作发起前后把涉及的新旧路径登记进 `touchedPathsRef`（TTL 8s），覆盖事件迟到的窗口；
  - **IPC 失败统一处理**（新建 `src/lib/fs/ipcGuard.ts`）：`reportFailure` 统一日志 + 人话转译 + error Toast；`reportGone` 统一「文件已消失」剔除（摘 `importedPathsRef`、收敛选中、同步重复分组，收藏 / 标签按路径有意保留）；重命名（单张 / 批量）、删除、移动、导出失败中的 ENOENT 项自动剔除且不进重试集合；
  - **K2 中间态**：跨卷移动「复制成功但删源失败」返回 `partial`，列表条目不动，Toast 说明并支持「重试」——重试回传 `priorTargets`（源路径 → 已落盘副本），主进程只幂等补删源，绝不重复复制；
  - **导入失败可重试**：扫描 / stat 失败不再被吞（K1），根目录级错误与「空目录」区分，部分失败给「N 个子目录 / M 个文件无法读取」+ 重试（重跑扫描按路径去重只补漏项）；文件选择导入的 `failedPaths` 单独提示并可重试；
  - **并发保护补齐**：移动操作补上与重命名 / 删除一致的提交锁（连点防护）。
  - 验证：`npm run typecheck` + `npm run build` 零错误。

### N7 · 封面落地（或移除半成品）

- **具体描述**：「设为封面」目前只加一个角标，没有任何地方消费它，属于半成品。二选一——
  - 方案一：让封面真正产生价值（「所有照片」顶部或空状态展示当前封面）；
  - 方案二：先移除该功能，避免半成品占位。
- **优先级**：低
- **预计完成时间**：v0.5.1（与 N5 同批收尾）
- **负责人**：维护者
- **验收标准**：封面有明确用途，或入口被移除且无残留 UI。
- **完成说明（2026-09-12）**：采用**方案二**。已移除「设为封面」右键入口、网格卡片「封面」角标与文件名右侧占位、数据模型（`Photo.isCover` / `PersistedConfig.cover`）及 `coverRef` 状态；重命名 / 移动 / 删除时的按路径迁移与落盘逻辑同步删除 `cover` 分支，无残留 UI。旧 `config.json` 中残留的 `cover` 字段不再被读取，不影响任何功能。

### N8 · 库来源持久化与恢复

- **具体描述**：当前以「打开文件夹 / 添加文件」为一次性入口，重启后不自动恢复（侧栏「最近打开」可手动重开）。但整理照片天然是跨会话的事，每次开机重找目录会把产品降级成「一次性批处理工具」。目标：把已打开的目录与已添加的文件存为常驻来源，重启后能一键（或经确认后）重建图库。
- **优先级**：高
- **预计完成时间**：v0.6.1
- **负责人**：维护者
- **验收标准**：
  - 退出时记录全部来源路径与导入时间，重启后侧栏「文件夹」完整还原；
  - 启动时给出「恢复上次的图库（N 个目录）?」，确认后后台重扫，复用现有进度浮层与取消链路；
  - 来源已不存在 / 卷未挂载时单独标注为「不可用」，可移除或重新定位（重定位能力见 N10），不静默丢来源；
  - 恢复后收藏 / 标签 / 时间修正 / AI 结果按路径全部命中。
- **完成说明（2026-09-13）**：
  - **来源记录**（新增 `LibrarySource`：路径 / 目录或文件 / 加入时间 / 最近导入时间，落 `config.json` 的 `sources`）：打开文件夹、添加文件、拖入文件或文件夹，凡导入成功即自动登记（按路径去重、最近使用在前、上限 200 条）；「清空照片列表」同步清空来源（语义是重新开始，不留下次启动又把刚清掉的库恢复回来）。旧配置没有 `sources` 时，把原「最近打开」的目录升级为文件夹来源（顺序保留），升级后首次启动即会收到恢复询问。合并 / 校验 / 迁移为纯函数（新增 `src/lib/persistence/sources.ts`）。
  - **启动恢复**：配置读完且列表为空时弹出「恢复上次的图库」确认框（列出文件夹名、来源总数与不可用数量，每会话只问一次；「暂不恢复」后仍可在侧栏逐个打开）。确认后按来源逐项重扫 —— 目录走 `scanDirectory` + `ingestFiles`，单独添加的文件一次 `statFiles` 后整批入库；进度浮层复用导入遮罩（显示「正在扫描 / 恢复 第 i/N 个来源」与逐目录进度），取消链路复用 `handleCancelLoading`（主进程 `cancelScan` + 渲染层 AbortController），取消后已入库部分保留；结束后汇总 Toast，并让第一个可用目录接管目录监听。
  - **侧栏「文件夹」区**（取代原「最近打开」）：文件夹逐个列出（点击 = 重新扫描补齐，悬停 ✕ = 移除来源），单独添加的文件聚合为一行（点击 = 原地补回，✕ = 全部移除）；新增主进程 `check-paths` IPC，启动时后台探测全部来源，不存在 / 卷未挂载的标为「不可用」（橙色 + 感叹号，悬停说明原因），点它给出「移除来源」入口 —— 不静默丢来源。「重新定位」留给 N10。
  - **不丢关联**：来源只增不减（除非显式移除）；移除来源时同步把该来源已加载的列表项移出（塌陷动画 + 收敛选中 + 同步重复分组 + 停止相关监听），但**不清**收藏 / 标签 / 时间修正 / AI 结果 —— 重新打开同一目录按路径全部命中；移出项的 `importedPaths` 登记一并摘除，「移除 → 重新添加」不产生重复条目。
  - 验证：`npm run typecheck` + `npm run build` 均通过。

### N9 · 操作日志与单次撤销（F2 降级版）

- **具体描述**：F2 的完整撤销栈成本过高，先落地降级版，解决「敢不敢用」——
  - 每次文件操作（重命名 / 删除 / 移动 / 导出）追加一行到 `userData/operation-log.jsonl`：`{ 时间, 类型, 旧路径, 新路径, 结果 }`；
  - 菜单「文件」下提供「最近操作」面板，可翻页、按类型过滤；
  - 仅支持**撤销最近一次删除**（从系统回收站取回）与**最近一次重命名**（反向改名），不做多级栈。
- **优先级**：高
- **预计完成时间**：v0.6.1
- **负责人**：维护者
- **验收标准**：
  - 删除后可在面板里「取回」，文件回到原路径；回收站已被系统清空时给出明确说明，不静默失败；
  - 重命名后可反向改回原名，遇同名沿用既有「追加序号」规则；
  - 日志按条数上限滚动（如 2000 条），不无限增长；写入沿用「临时文件 + rename」的原子写；
  - 撤销失败逐条进 Toast 说明具体原因，不吞错。

### N10 · 配置备份与路径重连

- **具体描述**：`config.json` 单点承载收藏 / 标签 / 时间修正 / 智能相簿 / 视频元数据 / AI 结果，坏了全丢；且文件一挪路径，全部按路径记录的关联即失联。两件事一起做——
  - **备份**：启动时滚动保留上一版 `config.json.bak`；设置里提供「导出配置 / 导入配置」；
  - **重连**：来源目录或文件被挪走后提供「重新定位」入口，指向新路径，按相对路径批量迁移该前缀下的全部记录。
- **优先级**：高
- **预计完成时间**：v0.6.1
- **负责人**：维护者
- **验收标准**：
  - 手动删掉 `config.json` 后可从 `.bak` 恢复；
  - 导出再导入一轮后，收藏 / 标签 / 相簿 / 时间修正 / AI 结果逐项一致；
  - 整个照片目录从 A 挪到 B 后，「重新定位」一次即全部续上；
  - 重定位遇冲突（新旧记录都存在）时给出选择，不静默覆盖。

---

## 三、功能改进点（F）

### F1 · 手动相册

- **具体描述**：自由挑选照片归入相簿（当前只有「条件相簿 / 智能相簿」，无法手动指定内容），与智能相簿互补。
- **优先级**：中
- **预计完成时间**：v0.6（按需）
- **负责人**：维护者

### F2 · 撤销安全网

- **具体描述**：仅覆盖「最近一次重命名 / 删除」的回退（删除 = 从回收站取回，重命名 = 反向改名），不做完整操作栈。建议 N5 完成后评估。
- **优先级**：中
- **预计完成时间**：v0.6
- **负责人**：维护者

### F3 · 浏览形态补充

- **具体描述**：~~时间线视图~~（已由「时光画廊」实现并发布）；剩余两项——
  - 地图式「按地点浏览」（复用已解析的 EXIF GPS，或先做轻量版，见 X4）；
  - 幻灯片强化（过渡动效 / 间隔可调）。
- **优先级**：低
- **预计完成时间**：v0.6
- **负责人**：维护者
- **完成说明（2026-09-12）**：两项均已落地，版本推进至 v0.6.0——
  - **按地点浏览**（X4 的轻量版；新增 `components/map/LocationMap.tsx` + `lib/geo/`）：
    - 整页地图视图，数据源为导入时已回填的 EXIF GPS（与时光画廊同源的「非隐藏全部照片」）；
    - 底图不依赖在线服务 / 地图 SDK：由 Natural Earth 110m 陆地轮廓栅格化出的 **0.5° 位掩码**（`lib/geo/landMask.ts`，内嵌约 43KB）在 canvas 上逐格填充 —— 世界视图下是均匀细网，放大后连成陆块、海岸线浮现；叠加经纬网与**城市名标注**（内嵌约 1700 条城市，按重要度 + 离视野中心距离贪心取舍，放不下的自动省略）；
    - **离线地名索引**（`lib/geo/places.ts` + `placeIndex.ts`）：「这是哪儿」不再只有经纬度 —— 25km 内直接报地名（深圳 · 广东）、150km 内「深圳附近」、更远给「距 敦煌 192 公里」的距离参照；数据来自 Natural Earth 10m Populated Places（自带 NAME_ZH 中文名，中国城镇 425 条全收录），1° 网格索引 + 结果缓存，供地图标签（**光点旁标出所在地名**）、悬停预览、地点照片条共用，不做任何在线逆地理编码；
    - 光点按屏幕距离聚合（46px 网格）：单张一颗小圆，多张按对数放大并标数字、双层柔光；悬停显示该地点的照片预览（最多 4 张缩略图）与地名、坐标，点击拉出地点照片条（**单张直接进 QuickLook**），进入预览后翻页范围收窄为该地点（`quickLookScope`，关闭即释放）；
    - 交互：滚轮以光标为锚点缩放、拖拽平移（rAF 节流 + 拖拽期底图降密度）、双击放大、`+ / - / 0` 与方向键键盘操作、顶栏「回到全部地点」取景复位；左下角给「视野内 N 张」读数；
    - 文案与空状态：顶栏「N 个地点 · M 张照片 · 另有 X 张未记录地点」，无 GPS 时说明原因与开启方式；地点数按 0.5° 网格统计，不随缩放跳动；
    - 入口在侧栏「视图」区（时光画廊与相似照片之间），无照片时置灰。
  - **幻灯片强化**（`components/detail/QuickLook.tsx`）：
    - 间隔从固定 3s 改为 **2 / 3 / 5 / 8 / 12 秒**可选：底部工具栏选择器（`3s` 按钮 → 弹出菜单），选择即写入 `localStorage`（`pm:slideshow-interval`）跨会话记忆；顶栏「幻灯片播放中」同步显示当前秒数，播放中调整立即生效；
    - **交叉淡入过渡**：翻页时把上一张的定格快照（连同当时的缩放 / 平移 / 旋转变换）垫在底层，新图就绪后两层一起淡出淡入；新图加载慢时旧图会一直垫到就绪（6s 兜底），不会中途抽走画面；涉及视频的切换不启用（由播放器接管）；快照层的尺寸规则与被替换的主图完全一致，过渡瞬间不做缩放跳动；
    - 播放期间控件常显（并清掉播放开始前挂起的隐藏计时器），随时能暂停 / 改间隔。
  - 验证：`npm run typecheck` + `npm run build` 均通过；前端产物增加约 44KB（掩码数据，gzip 后约 20KB），运行时零网络请求。

### F4 · 元数据写入

- **具体描述**：把应用内的拍摄时间修正 / 标签真正写回文件 EXIF（当前只改应用内记录）。需引入可写 EXIF 库，改动面较大。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F5 · AI 增强

- **具体描述**：批量分析选中项、按 AI 标签自动成组（AI 结果已持久化并纳入搜索）。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F6 · 实况照片合并

- **具体描述**：把「图片 + 配对视频」视为一条记录（列表只显示图片、播放时可在静图与动态之间切换），避免配对视频在「视频」分类里重复出现。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F7 · AI 结果消费出口

- **具体描述**：AI 标签目前只能参与搜索，生成了没人看，2000 条缓存躺在盘里。优先于「批量分析」补的是消费场景——
  - **标签云 / 标签侧栏**：按出现频次排列，点击即筛选，一眼看出「我的照片里有什么」；
  - **「未分析」筛选条件**：与批量分析队列形成「跑完 → 看结果 → 再跑」的循环；
  - **批量分析队列**（吸收 F5 的批量部分）：多选后排队执行，带断点续跑、失败重试与逐项进度，可中途暂停。
- **优先级**：中
- **预计完成时间**：v0.6.2
- **负责人**：维护者

### F8 · 重复检测闭环

- **具体描述**：检测结果只到「删 / 留」，缺最后一百米——
  - **忽略此组 / 标记为不是重复**：误报无处排泄，用户会掉头不用；忽略记录按内容指纹持久化，重跑不再出现；
  - **只检测选中项**：比「全库 / 同目录」更贴合实际（刚导入的一批先查一遍）；
  - **清理报告**：删完给一份「释放 X GB，N 张」的留存记录，这是这类工具唯一的成就感来源。
- **优先级**：中
- **预计完成时间**：v0.6.2
- **负责人**：维护者

### F9 · 未整理收件箱

- **具体描述**：缺一个驱动「整理完没有」的机制。侧栏加一个入口，聚合四类——无拍摄时间、无 GPS、缩略图解码失败（疑似损坏）、体积异常（过大 / 过小）。每类可直达并批量处理（调整日期 / 手动指定地点 / 导出 / 删除）。没有它，用户永远不知道整理完没有。
- **优先级**：中
- **预计完成时间**：v0.6.2
- **负责人**：维护者

### F10 · 归档备份（保结构增量复制）

- **具体描述**：与 `ExportModal` 的「转格式到某目录」不同，缺的是「原样归档」——把整个库或选中项**按原目录结构**复制到备份盘，目标已存在且 size + mtime 一致则跳过，支持断点重跑。整理完的最后一步就是归档，现在这一步是断的。
- **优先级**：中
- **预计完成时间**：v0.6.2
- **负责人**：维护者

### F11 · 文件夹树浏览

- **具体描述**：侧栏只有平铺的来源文件夹，缺树形浏览。整理场景下「按目录看」是刚需，也能暴露「这个文件夹为什么这么大」。以导入来源为根展开子目录，节点带计数，点击即把范围收窄到该子目录（与现有筛选共存）。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F12 · 地点 → 事件（自动聚合「旅行」）

- **具体描述**：按地点浏览（F3）目前只能「看」，缺上层组织。按「时间连续 + 空间聚集」把照片自动切成若干次出行，每份给出时间跨度、地点数与张数，可一键存为智能相簿。这是把地图从「看」变成「用」的关键一步，复用已落地的 `lib/geo/` 与聚类逻辑。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F13 · 地点改名与地图批量操作

- **具体描述**：地图目前只能进 QuickLook，无法批量；地名由内置 `places.ts` 自动推断，用户改不了——
  - **手动改名**：给点位 / 簇指定名称（覆盖自动地名），存进 `config.json`。个人应用里「我给这个地方起名」比「自动识别」更贴合本地定位；
  - **簇可多选**：点选一个簇 → 全选该地点照片 → 存为相簿 / 导出 / 加标签。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F14 · 网格状态可读性

- **具体描述**：收藏、用户标签、时间已修正、AI 已分析这四种状态在网格卡片上完全不可见，只能逐张打开确认。需要在卡片角标层给出轻量指示，可随缩放档位决定是否显示，避免小尺寸下糊成一片。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F15 · 缩略图空闲预热

- **具体描述**：按需生成很省内存，但滚动时仍要等一次解码。空闲时（无滚动 / 无操作的间隙）为前 N 屏预热缩略图，让滚动接近「秒出」。需与主进程并发闸门与内存看门狗联动，内存紧张时立即停止。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

### F16 · 存储占用分析

- **具体描述**：低成本高感知的统计视图——按年份 / 按目录 / 按格式的占用分布，最大单文件 Top N，视频占比。复用已入库的 `size` 与目录前缀，不需要额外扫描磁盘。
- **优先级**：低
- **预计完成时间**：v0.6+（按需）
- **负责人**：维护者

---

## 四、已知问题与修复计划（K）

> **来源**：2026-09-12 两轮全量审查（第一轮逻辑审查 + 第二轮体验审查）遗留的 P2 级问题，**均不会丢数据**，但结果不准或体验受损。多数与 N5 同源，做 N5 时一并处理更划算。
> **修复计划**：按四个批次推进，每条含涉及文件与（已明确的）修复方案。

### 批次 A · 稳定性与容错（与 N5 同批，目标 v0.5.1）

- [x] **K1 · 扫描 / stat 失败被吞成空结果**
  - 描述：无权限目录只提示「不包含任何图片或视频」，无法区分「为空 / 不存在 / 全部失败」；导入中个别文件被丢弃无任何说明。（`electron/main.js` `scan-directory`、`stat-files`）
  - 优先级：中 · 预计完成时间：v0.5.1 · 负责人：维护者
  - 完成说明（2026-09-12，随 N5）：`scanDirectory` 返回 `{ files, failedDirs?, failedFiles?, cancelled?, error?, errorCode? }`，`stat-files` 返回 `{ infos, failedPaths, error? }`；渲染层同步适配（`loadDirectory` / 文件选择导入 / 拖放分支），根目录级错误与空目录明确区分，部分失败带可重试提示（重跑只补漏）。

- [x] **K2 · 跨卷移动「复制成功但源删除失败」没有中间态**
  - 描述：从只读卷移动时目标已生成副本却整体记为失败，Toast 的「重试」会导致再复制一份。（`electron/main.js` `moveAcrossDevices`）
  - 优先级：中 · 预计完成时间：v0.5.1 · 负责人：维护者
  - 完成说明（2026-09-12，随 N5）：`moveAcrossDevices` 删源失败不再整体抛错，返回 `partial` 中间态；`move-files` 支持幂等重试（`copyFile` EEXIST 时两侧 size 相等则直接补删源，不等则重新取唯一名），渲染层对 partial 项不更新列表、Toast 说明并回传 `priorTargets` 只补删源，不产生重复副本。

- [ ] **K3 · 主进程存在同步阻塞事件循环的调用**
  - 描述：`nativeImage.createFromPath / toJPEG`（缩略图、哈希兜底、元数据兜底、`copy-image`）、`fs.existsSync / statSync`（`buildUniquePath`）、大 config 的 `JSON.stringify`。
  - 修复方案：解码移入 `utilityProcess`，至少把大配置序列化与写盘节流。（`electron/main.js`）
  - 优先级：中 · 预计完成时间：v0.5.1 · 负责人：维护者

- [ ] **K4 · ErrorBoundary 覆盖不全，主进程无未捕获异常自恢复**
  - 描述：局部边界只覆盖重复检测 / 时光画廊 / 图库 / 详情面板；Toolbar 或弹层渲染抛错会整屏降级到顶层兜底，而顶层「重试」以相同入参重挂载多半再次抛错，实际只能重新加载或重启。（`src/main.tsx`、`electron/main.js`）
  - 优先级：低 · 预计完成时间：v0.5.1 · 负责人：维护者

- [ ] **K5 · `version` 字段只写不读**
  - 描述：`config.json` / `ai-cache.json` 的 `version` 字段没有版本校验与迁移入口。（`src/lib/persistence/persistence.ts`、`electron/main.js` `readStore`）
  - 优先级：低 · 预计完成时间：v0.5.1 · 负责人：维护者

- [ ] **K6 · `before-quit` 中窗口尺寸落盘未 `await`**
  - 描述：退出前可能写不完（macOS 上 `window-all-closed` 不退出，影响很小；写队列锁已修好，此条仅剩时序兜底）。（`electron/main.js` `persistWindowBounds`）
  - 优先级：低 · 预计完成时间：v0.5.1 · 负责人：维护者

### 批次 B · 数据与结果正确性（目标 v0.5.2）

- [ ] **K7 · AI 缓存 key 只绑定路径，不绑定文件内容与模型**
  - 描述：换模型、或用外部工具覆盖编辑图片（路径不变）后，命中的仍是旧结果且不会失效。（`src/App.tsx` `aiCacheRef` 读写）
  - 修复方案：key 改为 `${path}|${size}|${mtime 取整}|${model}`。
  - 优先级：中 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K8 · ai-cache 淘汰是插入序 FIFO，不是 LRU**
  - 描述：命中不刷新顺序，最常看的条目反而最先被淘汰。（`src/lib/persistence/aiCache.ts`）
  - 修复方案：命中时 `delete + set` 重新入队。
  - 优先级：中 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K9 · 重复检测并查集取传递闭包，组内出现低于阈值的配对**
  - 描述：A–B、B–C 达标但 A–C 不达标时三者同组，C 的角标显示「相似 69%」而顶栏写「≥ 80%」，自相矛盾。（`src/utils/index.ts` UnionFind、`src/components/duplicate/DuplicateDetector.tsx`）
  - 优先级：中 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K10 · 体积预筛 ±10% 会漏掉体积差大的重复**
  - 描述：同一张图存成 PNG 与 JPEG 检测不到，而文案仍声称「已跳过体积唯一、不可能相似的图片」。（`src/utils/index.ts` `selectHashCandidates`）
  - 优先级：中 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K11 · 重命名预览不预演磁盘上已存在的同名文件**
  - 描述：预览显示 `照片_001.jpg`，实际落盘为 `照片_001-1.jpg`，只在事后用 Toast 说明。（`src/components/modal/RenameModal.tsx`、`src/App.tsx` 批内 `usedNames`）
  - 优先级：低 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K12 · 点「取消导出」仍会多写完当前这一张**
  - 描述：`cancelRef` 只在循环头判断，无法中断 in-flight 的写盘。（`src/components/modal/ExportModal.tsx`）
  - 优先级：低 · 预计完成时间：v0.5.2 · 负责人：维护者

- [ ] **K13 · 右键菜单对「视频 / 混合选择」的可用性与计数不对**
  - 描述：选中「2 图 + 1 视频」右键图片 → 显示「导出 3 张」实际只导 2 张；右键视频则完全没有「导出」（即使组内有图片）。另：同一次多选里「收藏 / 复制图片」只作用于右键那一张且不带数量提示，与「隐藏 / 移动 / 重命名 N 项」的作用域不一致。（`src/lib/contextMenuActions.ts`）
  - 优先级：中 · 预计完成时间：v0.5.2 · 负责人：维护者

### 批次 C · 交互与体验（v0.5.x 随批消化）

> **进度**：10 / 13 已完成（K14 · K15 · K17 · K18 · K19 · K21 · K22 · K23 · K24 · K25）。
> 余 **K16 · K20 · K26** 三条，均涉及交互取舍（选中态是否保留浏览控件 / 分类跳转是否清筛选 / 相簿是否支持改名），待定策略后再动手。

- [x] **K14 · 网格视图上下键按固定列数跳步，混排宽高比时会跳错列**
  - 描述：justified 布局每行张数随宽高比变化，`itemsPerRow` 只取第 0 行。（`src/components/grid/ImageGrid.tsx`）
  - 修复方案：按 `rowOfItem` 找当前行号，再取下一行中 `left` 最接近的 cell。
  - 优先级：中 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：`VirtualGrid` 新增 `getVerticalNeighbor(id, direction)`，按 `rowOfItem` 定位当前行，在相邻行的 cell 中取「水平中心最接近」的一张；`ImageGridHandle` 透出给 `App`，`handleArrowNavigation` 的上下键改为向布局查询落点，列表视图与左右键保持顺序步进，首 / 末行不再用固定列数硬跳。`gridColumns` 状态与上报随之移除（上报链路保留为可选参考）。

- [x] **K15 · 从「时光画廊 / 重复检测」返回图库后，滚动位置丢失被拉回顶部**
  - 描述：`mainView` 变化会卸载图库子树，`VirtualGrid` 的 `scrollTop` 随之清零；时光画廊自己用模块级变量记住了位置，图库没有。（`src/App.tsx` `mainView` 切换）
  - 修复方案：沿用时光画廊已验证的 `savedScrollTop` 模式。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：`ImageGrid` 内为网格 / 列表各加一个模块级 `savedGridScrollTop` / `savedListScrollTop`，滚动时持续记录；重挂载后等布局算出内容高度（网格 `totalHeight > 0`、列表 `items.length > 0`）再恢复一次，避免首帧容器为空被浏览器夹回 0。

- [ ] **K16 · 一旦有选中项，排序与缩放入口整条消失**
  - 描述：浏览条与选择条二选一渲染，选中后必须先 `Esc` 才能再调排序 / 缩放。（`src/components/grid/ImageGrid.tsx`）
  - 修复方案：把排序 / 缩放控件抽成共用 JSX，在选择条里也渲染。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者

- [x] **K17 · 缩放滑块在窄窗口（< 1024px）被断点隐藏**
  - 描述：`hidden lg:flex` 导致窄窗口下网格大小完全无入口。（`src/components/grid/ImageGrid.tsx`）
  - 修复方案：去掉断点限制，或补 `⌘+ / ⌘-` 快捷键。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：断点由 `hidden lg:flex` 放宽到 `hidden sm:flex`（≥ 640px 均可见），覆盖实际会用到的最小窗口宽度，不再出现「完全没有网格大小入口」的情况。

- [x] **K18 · 元数据回填导致网格持续重排、卡片跳动、滚动位置漂移**
  - 描述：入库时没有 `dimensions`，首屏按 4:3 兜底铺排，随后每 400ms 一批回填尺寸，行高与行数反复变化。（`src/components/grid/ImageGrid.tsx` `photoAspect`、`src/App.tsx` `loadMetadata`）
  - 修复方案：布局变化后把「首个可见照片」重新对齐到视口顶部。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：`VirtualGrid` 持续记录「视口顶部那一行」的照片 id 与其相对偏移；当 `items` 变为同一批照片（id 顺序不变，即尺寸回填 / 收藏切换这类几何或无关键变化）而重排后，用 `useLayoutEffect` 把该行按原偏移对回原位。只在同序列变化时出手，排序 / 筛选换序不干预；偏移为 0 或位置未变时不写 scrollTop。

- [x] **K19 · 弹层内的批量操作无进度、确认按钮不置灰**
  - 描述：大量文件时表现为「卡死」，用户会重复操作；导出已有进度 + 取消，是不对称的。（`RenameModal` / `DeleteConfirmModal` / `handleMoveSelected`）
  - 修复方案：复用 `ExportModal` 的进度模式。
  - 优先级：中 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：新增统一的批量操作反馈（`beginFileOp` / `reportFileOp` / `endFileOp`，`App.tsx`）——同步置 busy 让 `RenameModal` / `DeleteConfirmModal` 的确认按钮与 Esc / Enter 失效，超过 320ms 才升起复用 `LoadingOverlay` 的进度遮罩（可选 `title` / `hint`，不传 `onCancel` 则不显示取消）拦截连点。进度来源：重命名按项上报、删除给 `movePhotosToTrash` 增加 `onProgress` 逐项回调、移动是主进程一次性完成故用不确定态；小批量瞬间完成不闪遮罩。

- [ ] **K20 · 侧栏分类跳转与相簿跳转行为不一致**
  - 描述：点「收藏夹」保留搜索词与高级筛选（侧栏只高亮单一分类，看不出还叠了条件）；点智能相簿却清空搜索词；且「收藏夹 + 媒体类型 = 视频」时侧栏两项都不高亮，出现无高亮的空白态。（`src/App.tsx` `handleSelectNav` vs `handleSelectAlbum`）
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者

- [x] **K21 · 破坏性快捷键与 macOS 习惯冲突**
  - 描述：Finder 里 `⌫` 是重命名、`⌘⌫` 才是移到废纸篓；本应用单按 `⌫` 就弹删除确认。（`src/App.tsx`）
  - 修复方案：改为 `⌘⌫`（可保留 `Delete` 兼容），并在 README / 快捷键总览说明。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：删除确认改为 `⌘⌫`（`Delete` 键保留兼容），单按 `⌫` 不再触发删除；右键菜单快捷键提示、选择条标题、快捷键总览与 README 同步改为 `⌘⌫`。

- [x] **K22 · 调整相似度阈值后整页闪切到进度视图，无法连续微调**
  - 描述：参数面板被 `!isProcessing &&` 包裹，重新分组时结果列表整个消失。（`src/components/duplicate/DuplicateDetector.tsx`）
  - 修复方案：区分「首次算指纹」与「仅重新分组」，重分组时保留结果列表只显示细进度条。
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：`useDuplicateDetection` 新增 `isRepartitioningDuplicates` 与 `handleCheckDuplicates(…, 'repartition')` 模式——参数变化自动重跑走该模式，不清空结果、不进入全屏进度，只叠细进度条；被新一轮取代的中止不再清空结果。`DuplicateDetector` 顶栏副标题与顶部细进度条据此展示，重新检测按钮在两种进行态下都置灰。首次扫描 / 无结果时仍走全量。

- [x] **K23 · 首屏入场 `introDelays` 永不被清除**
  - 描述：清除 timer 的 effect 被 `photos` 变化不断打断，卡片重新挂载会重播淡入动画。（`src/components/grid/ImageGrid.tsx`）
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：定时器改为存进 `introTimerRef`，只在卸载时清理，不再随 `photos` 变化的清理函数被提前清掉，入场标记到点即撤销。

- [x] **K24 · 深度滚动后筛选缩短列表，虚拟列表被逐帧夹回顶部**
  - 描述：触发路径：深度滚动后在搜索框输入只命中少数条目的关键词，期间整屏空白。（`src/components/grid/ImageGrid.tsx` `VirtualList`）
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：窗口起点 `startIndex` 夹取到 `[0, items.length-1]`。此前起点可能远大于缩短后的列表长度，`slice` 为空导致整屏空白、占位行又按旧起点撑高使浏览器无法回落；夹取后至少渲染末尾一行，内容高度随之收缩，滚动条自然回到有效区间。

- [x] **K25 · 时光画廊新增月份区块后「当前月份」判定错乱**
  - 描述：代码假设 `monthAnchorsRef` 的 Map 插入序等于视觉顺序，但新挂载的月份区块追加到 Map 末尾却位于 DOM 上方。（`src/components/timeline/TimelineGallery.tsx`）
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者
  - 完成说明（2026-09-12）：「当前月份」判定改为遍历渲染顺序 `orderedMonthKeys`（与 DOM 顺序一致）并取最后一个越过边界的区块，遇到未越过边界的即停止，不再依赖 Map 插入序。

- [ ] **K26 · 智能相簿缺重名校验与重命名入口**
  - 描述：创建同名相簿无提示，已建相簿无法改名。（`src/components/modal/SaveAlbumModal.tsx`、`src/App.tsx` `handleDeleteAlbum` 附近）
  - 优先级：低 · 预计完成时间：v0.5.x · 负责人：维护者

### 批次 D · 视觉与身份（v0.6.x 随批消化）

> **背景**：当前 UI 是标准 macOS 风（毛玻璃 / 圆角 / CSS 变量），与系统「照片」高度同质，还没有自己的身份。个人应用恰恰最该有性格——它不需要讨好任何人。这批条目不追求「好看」，追求「一眼认得出」。**来源**：2026-09-13 产品经理视角评审。

- [ ] **K27 · 首屏空状态与设计语言同质化**
  - 描述：六种空状态（完全为空 / 收藏夹为空 / 搜索无结果 / 筛选后为空 等）共用同一套模板（图标 + 一行字 + 按钮）。完全为空的首屏既是产品的第一印象，也是最大的一块未被设计的画布。（`src/components/grid/` 空状态相关）
  - 修复方案：首屏单独设计，其余态保持克制但区分语气。
  - 优先级：低 · 预计完成时间：v0.6.x · 负责人：维护者

- [ ] **K28 · 整页视图未统一沉浸态**
  - 描述：时光画廊、按地点浏览、QuickLook 都是「照片主导」的场景，但只有 QuickLook 刻意保持深色底，前两者仍挂常规工具栏与主题色背景，三处不像同一个产品。（`src/components/timeline/`、`src/components/map/`、`src/components/detail/QuickLook.tsx`）
  - 修复方案：抽一套沉浸态（隐藏 chrome、暗底、内容铺满），三处共用。
  - 优先级：低 · 预计完成时间：v0.6.x · 负责人：维护者

- [ ] **K29 · 统计数字未被当作设计元素**
  - 描述：库规模、重复检测可释放体积、地点数这些最有说服力的数字，现在都是小字灰字，没有排版层级。（侧栏计数、重复检测顶栏、地图顶栏副标题）
  - 修复方案：给出数字排版规范（字号 / 字重 / 字距 / 单位处理），在关键位置统一套用。
  - 优先级：低 · 预计完成时间：v0.6.x · 负责人：维护者

- [ ] **K30 · 点阵底图语言未贯穿**
  - 描述：`src/lib/geo/landMask.ts` 的点阵语言是本项目唯一「别人没有」的视觉资产，目前只用在地图底图上；图标、空状态、加载态仍是通用描边图标。（`src/components/map/LocationMap.tsx`、侧栏图标、加载占位）
  - 修复方案：把点阵作为签名元素贯穿到首屏空状态、加载态与地图相关图标，形成产品唯一的视觉记忆点。
  - 优先级：低 · 预计完成时间：v0.6.x · 负责人：维护者

---

## 五、明确延后（X）

> 个人本地应用，以下项收益低或成本高，除非日后确有需要，否则不做。**预计完成时间均为：暂不排期。**

| 编号 | 事项 | 优先级 | 预计完成时间 | 负责人 | 暂缓理由 |
| --- | --- | --- | --- | --- | --- |
| X1 | API Key 用 `safeStorage` 加密 + 主进程代理 | 低 | 暂不排期 | 维护者 | 个人自用、本地单机，泄露风险可接受 |
| X2 | 移除 `no-sandbox`、开启 sandbox、CSP、IPC 路径校验 | 低 | 暂不排期 | 维护者 | 安全加固对个人使用收益低，改动面大 |
| X3 | 基础图片编辑（裁剪 / 翻转 / 调色） | 低 | 暂不排期 | 维护者 | 系统「预览 / 照片」已够用，重复造轮子 |
| X4 | 地图视图（EXIF GPS 展示） | 低 | 已完成（并入 F3 轻量版） | 维护者 | 已随 F3 落地：离线点阵底图 + 点位聚合，未引入地图 SDK / 不在线瓦片；如需街道级细节再议 |
| X5 | 跨平台打包（Win/Linux）+ 自动更新 | 低 | 暂不排期 | 维护者 | 只在 macOS 自用 |
| X7 | `App.tsx` 大拆分 + 开启 `strict` + 引入 `@types/react` | 低 | 暂不排期 | 维护者 | 纯工程收益，风险高于当前收益 |
| X8 | 测试 / ESLint / CI | 低 | 暂不排期 | 维护者 | 个人项目，靠手动验证 + `tsc` + `build` 已足够 |
| X9 | 十万级库 Worker 化压测与调优 | 低 | 暂不排期 | 维护者 | 除非库规模真的到十万级 |
| X10 | 云同步 / Google Photos 等外部集成 | 低 | 暂不排期 | 维护者 | 与「纯本地」定位冲突 |
| X11 | 视频重编码导出 / 转码预览 | 低 | 暂不排期 | 维护者 | 依赖 ffmpeg，体积与复杂度陡增；原文件可直接复制 |

> 编号说明：X6 已并入 X7，编号保留空缺以免破坏历史引用。

---

## 六、里程碑与进度

| 里程碑 | 范围 | 状态 |
| --- | --- | --- |
| **v0.4 顺手版** | N1 状态持久化、N2 重命名不覆盖、N3 搜索与筛选增强、N4 体验细节打磨、N6 视频体验完善，以及隐藏 / 智能相簿 / 用户标签 / 调整日期与时间 | ✅ 已发布（2026-09） |
| **v0.6.0 健壮版** | 全量代码审查与高危修复（两轮）、应用内 AI 设置、打包瘦身 | 🔨 开发中（当前版本号） |
| **v0.5.1** | N5 稳定性加固 + K 批次 A（N7 封面已采用方案二移除） | 🔨 进行中（N5 / K1 / K2 已完成，批次 A 其余项待做） |
| **v0.5.2+** | K 批次 B / 批次 C 随批消化（批次 C 已完成 K14 / K15 / K17 / K18 / K19 / K21 / K22 / K23 / K24 / K25，余 K16 / K20 / K26） | 📋 规划中 |
| **v0.6.0** | F3 浏览形态补充（按地点浏览 + 幻灯片强化） | ✅ 已完成（2026-09-12） |
| **v0.6.1 敢用版** | N8 库来源持久化与恢复 ✅、N9 操作日志与单次撤销、N10 配置备份与路径重连 | 🔨 进行中（N8 已完成） |
| **v0.6.2 闭环版** | F7 AI 消费出口、F8 重复检测闭环、F9 未整理收件箱、F10 归档备份 | 📋 规划中 |
| **v0.6+** | F1 / F2 / F4–F6、F11–F16 按需；K 批次 D（视觉与身份）随批消化 | 📋 规划中 |

---

## 七、已完成事项存档

> 功能现状基线以 [`README.md`](./README.md)「功能说明」为准，本文件不再重复维护。

- **2026-09-10**：N1（状态持久化）、N2（重命名不覆盖）、N3（搜索与筛选增强）、N4（体验细节打磨）、N6（视频体验完善）完成，v0.4.0 发布。
- **2026-09-12 第一轮 · 全量逻辑审查**：修复 1 项 P0（重命名后收藏 / 标签 / 封面 / AI 缓存等按路径数据全部丢失）、10 项 P1（隐藏项泄漏、排序取值错误、写盘串行化与失败感知等）、6 项 P2，见 commit `982be77`。
- **2026-09-12 第二轮 · 操作体验审查**：修复 1 项体验阻断（拖放导入无反馈、进度永远不确定态）、9 项体验受损、7 项体验瑕疵，并复核第一轮全部修复无副作用；`npm run typecheck` 与 `npm run build` 均通过。
- **2026-09-12 · N5 稳定性加固（本地场景）**：目录监听（`fs.watch` 递归 + 防抖聚合 + 外部变动自动同步与汇总提示）、回环防护双保险（锁内暂存 + TTL 登记）、`ipcGuard` 统一 IPC 失败上报与「文件消失」自动剔除（重命名 / 删除 / 移动 / 导出接线）、导入失败可重试不留半状态、移动操作补齐提交锁；同源 K1（扫描 / stat 失败不再被吞）与 K2（跨卷移动 partial 中间态 + 幂等补删源）一并修复；`npm run typecheck` 与 `npm run build` 均通过。
- **2026-09-12 · 批次 C 交互与体验（两批，10 / 13 完成）**：
  - **低风险项先行**：K14 网格上下键改按真实行几何取落点、K15 返回图库保留滚动位置、K17 网格缩放滑块放宽断点、K21 删除改 `⌘⌫`（对齐 macOS）、K23 首屏入场标记到点即清、K25 时光画廊当前月份按渲染序判定；
  - **第二批**：K18 元数据回填时按锚点回正网格滚动位置、K19 重命名 / 删除 / 移动补齐进度与确认按钮置灰（`movePhotosToTrash` 增加逐项 `onProgress`，`LoadingOverlay` 支持自定义标题与不显示取消）、K22 调参改为保留结果列表的轻量重分组、K24 虚拟列表窗口起点夹取到有效范围消除空白；
  - 余 **K16 / K20 / K26** 涉及交互取舍，待定策略。`npm run typecheck` 与 `npm run build` 均通过。
- **2026-09-12 · F3 浏览形态补充（v0.6.0）**：新增「按地点浏览」整页地图视图（内嵌 0.5° 陆地掩码底图、城市名标注、EXIF GPS 光点与屏幕聚合、**离线地名索引**（光点旁标地名 / 悬停与照片条给地名与距离参照）、悬停预览、地点照片条与 QuickLook 地点内翻页、滚轮 / 拖拽 / 双击 / 键盘交互、无 GPS 空状态与「未记录地点」提示）；QuickLook 幻灯片强化（间隔 2–12 秒可选且跨会话记忆 + 交叉淡入过渡 + 播放期控件常显）；`npm run typecheck` 与 `npm run build` 均通过。
- **2026-09-13 · N8 库来源持久化与恢复**：新增常驻来源（`config.json` 的 `sources`：路径 / 目录或文件 / 加入与最近导入时间，上限 200 条）——打开文件夹、添加文件、拖放导入成功后自动登记，旧配置的「最近打开」自动升级；启动时弹「恢复上次的图库」确认框，确认后按来源逐个重扫（复用导入进度浮层与取消链路，单独文件一次 `statFiles` 补回）；侧栏「文件夹」区支持点击重扫 / 补回与悬停移除，新增主进程 `check-paths` 在启动时后台探测可用性，不可用来源橙色标注并可移除；移除来源只摘记录（列表项随之移出），收藏 / 标签 / 时间修正 / AI 结果按路径保留，重新添加即命中。`npm run typecheck` 与 `npm run build` 均通过。

### 已核对无需复查的范围（两轮审查结论，避免重复排查）

- **IPC 契约**：27 个 `invoke` + 3 个事件的 channel 名、参数顺序、返回结构与 `global.d.ts` 完全一致（N8 新增 `check-paths`）。
- **主进程文件操作**：重命名的同名序号回退、删除走 `shell.trashItem`（确为回收站）、导出的 `buildUniquePath` 永不覆盖、导入 / 导出取消标志能真正中断且无残留。
- **AI 代理链路**：Key 只走主进程、`AbortController` 超时、`response.ok` 与空 content 检查、JSON 围栏容错、设置弹窗防竞态。
- **缓存与内存治理**：LRU 命中刷新与逐项淘汰、volatile / sticky 分级、缩略图并发闸门与同 key 去重、generation 防旧回写、内存看门狗与堆巡检带冷却。
- **筛选求值**：日期闭区间、大小 1024 进制、时长单位为秒、无 EXIF 文件正确排除；搜索全程 `includes`，无正则注入。
- **状态一致性**：侧栏与筛选面板共用状态、筛选变化后选择自动收敛、多选时用户标签有守卫、相簿条件序列化可逆。
- **导入取消链路**：三处配合正确，取消后不会回写。
- **QuickLook**：翻页边界、幻灯片定时器清理、切图重置、视频元素无泄漏。
- **体验面向**：多选语义、删除二次确认、虚拟滚动机制、缩略图按需层（含 onerror 回退）、六种空状态、Toast 行为、右键菜单定位、导出进度、按钮文案、时光画廊导航、详情面板多选只读展示。
