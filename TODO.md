# PhotoMinder 开发计划（个人使用版）

> 最后整理：2026-09-10
>
> **定位**：个人自用的本地照片 / 视频整理工具，macOS 为主。
> **优先级**：用起来舒服、稳定、不丢数据 ＞ 功能数量。
> **原则**：够用即止；非必要不引入复杂度（云端、多平台、多语言、大重构一律靠后）。
>
> 编号约定：**N = 近期就做** · **F = 后续按需** · **X = 明确延后（非必要不启动）**。

---

## 一、现状基线（已完成，不再排期）

**导入与浏览**
- 打开文件夹 / 多选导入 / 拖放导入（支持直接拖入文件夹；拿不到磁盘路径时降级为仅预览）
- 扩展名白名单：图片 12 种 + 视频 25 种；递归扫描（限制深度、跳过隐藏文件与符号链接、可取消）
- 导入可取消、进度浮层、按路径去重；虚拟滚动网格 + 列表双视图；按名称/大小/各类时间排序 + 日期分组吸附
- 侧栏导航：图库（所有照片 / 图片 / 收藏夹）+ **媒体类型**（视频 / 自拍 / 实况照片 / 截屏，仿 macOS 照片，带计数）、收藏标记、多选（⌘ / Shift / 方向键）
- 智能媒体类型识别（本地启发式，`mediaTypes.ts`）：截屏（文件名 / PNG+屏幕分辨率）、自拍（文件名 / 前置镜头 EXIF）、实况照片（同目录同名图片 + 视频配对）
- QuickLook：缩放（双击 1x/2x）、旋转、平移、翻页、页码、收藏、**幻灯片自动播放**；视频内嵌播放
- 按需缩略图（视口内请求、并发闸门、去重）+ 磁盘缩略图缓存；**视频首帧抓帧 + 回写缓存**
- **视频体验完善**（N6 · 2026-09-10）：播放器上报时长 / 分辨率并缓存持久化、网格时长角标、详情面板视频信息（时长 / 分辨率 / 容器）、首帧失败明确占位、无法解码时给出「在访达中显示」兜底

**搜索与筛选**
- 关键词搜索覆盖文件名 / 格式 / 相机机型 / **AI 标签与描述**
- 可组合筛选：收藏、媒体类型、日期范围（含预设）、相机/机型、格式、文件大小、视频时长；筛选面板 + 已启用条件条（逐条清除 / 一键清除），与侧栏分类共用同一状态（N3 · 2026-09-10）
- 筛选变化后自动收敛选择，避免误删被条件隐藏的条目

**文件管理**
- 单张就地重命名 + 批量重命名（格式化 / 替换 / 拍摄时间 / **乱码修复**，实时预览）
- **重命名不覆盖**：同名时主进程自动追加序号并回传最终文件名（N2 · 2026-09-10）
- 删除入系统回收站（逐张返回失败原因）；复制图片 / 复制路径 / 在访达显示 / 外部应用打开 / 设为封面
- **批量导出**：原格式 / JPEG / PNG / WebP，质量可调、目标目录、进度、可取消、同名自动加序号

**状态持久化（N1 · 2026-09-10 完成）**
- 主进程通用 JSON 存储：`userData/config.json`，临时文件 + rename 原子替换
- 按路径记录：收藏、隐藏、用户标签、拍摄时间修正、图库封面、视频元数据（时长 / 分辨率）
- 视图偏好：主题、视图模式、排序、网格缩放、媒体筛选、侧栏 / 详情面板开合；重复检测阈值与范围；最近打开的目录（侧栏「最近打开」一键重开）
- AI 分析结果：单独存 `userData/ai-cache.json`，上限 2000 条，超出淘汰最早
- 窗口尺寸与最大化状态由主进程记录，含「显示器拔掉后旧坐标落在屏幕外」的多屏保护

**整理与查看（借鉴 macOS 照片，2026-09-10）**
- **隐藏**：批量隐藏 / 取消隐藏，隐藏项默认不出现在所有视图，集中收在「已隐藏」
- **智能相簿**：把筛选条件存成命名相簿（侧栏「+」/ 条件条「存为相簿」），内容随图库实时求值，可应用 / 删除
- **用户标签**：详情面板增删标签（与 AI 标签分开维护），参与搜索并可作为筛选条件
- **调整日期与时间**：批量平移 / 设为指定时间（可保持相对间隔），仅改应用内记录并在详情面板标注「已修正」
- **配置持久化**：收藏 / 隐藏 / 标签 / 时间修正 / 智能相簿写入 `userData/config.json`（临时文件 + rename 原子替换），重启后保持

**重复检测 / AI / 体验**
- dHash 感知哈希（三级解码降级）+ 体积预筛 + 并查集聚类；可调阈值（80%–100%，默认 ≥ 90%）、「全库 / 仅同目录」
- **重复检测为整页视图**：进度 / 已用时 / 预计剩余 / 可取消，跳过与缓存复用统计，分组可展开，「只留每组原图」批量标记，可释放体积统计
- EXIF 详情（含方向 / GPS / 色彩空间）、AI 描述与标签（DeepSeek 视觉模型，主进程代理）
- 深色 / 浅色主题（色值全部走 CSS 变量）、右键菜单（按图片 / 视频动态裁剪）、错误边界（顶层 + 图库/详情/重复页局部）
- **Toast 支持操作按钮与失败重试**：删除 / 导出 / 导入失败可一键重试；位置移到底部，不再遮挡工具栏（N4 · 2026-09-10）
- **统一缓存治理**：真 LRU（volatile / sticky 分级）、统一释放、主进程内存看门狗 + 渲染进程堆巡检
- 依赖：Electron 44 / React 19 / TS 7 / Vite 8 / Tailwind 4；统一日志器（打包后静默）
- 仓库：`LICENSE`（MIT）、`.env.example`、版本号一致；`npx tsc --noEmit` 零错误、`npm run build` 通过

---

## 二、N · 近期就做（体验与稳定）

> 目标：让「每天打开就能顺手用」的部分闭环——**有记忆、找得到、不崩、视频不别扭**。建议里程碑 **v0.4 顺手版**。
> 进度：**N1（状态持久化）、N2（重命名不覆盖）、N3（搜索与筛选增强）、N4（体验细节打磨）、N6（视频体验完善）已完成**（2026-09-10）；剩余 **N5 / N7**。

### N5 · 稳定性加固（本地场景）　【中】
- **预期目标**：文件操作的异常兜底与恢复——外部已删除 / 移动的文件在操作时给出明确提示并自动从列表剔除；对目录做轻量监听（`fs.watch`）以感知外部增删；IPC 失败统一走 Toast；导入中途失败可续 / 可重试，不留半状态；重命名 / 删除并发保护。
- **验收标准**：模拟文件被外部删除 / 移动 / 权限变化，应用不崩、状态自洽、提示清晰。

### N7 · 封面落地　【低】
- **为什么**：「设为封面」目前只加一个角标，没有任何地方消费它，属于半成品。
- **预期目标**：二选一——要么让封面真正产生价值（「所有照片」顶部或空状态展示当前封面），要么先移除该功能，避免半成品占位。
- **验收标准**：封面有明确用途，或入口被移除且无残留 UI。

---

## 三、F · 后续按需（N 阶段稳定后再考虑）

- **F1 · 手动相册**：自由挑选照片归入相簿（当前只有「条件相簿」，无法手动指定内容），与智能相簿互补。
- **F2 · 撤销安全网**：仅覆盖「最近一次重命名 / 删除」的回退（删除=取回，重命名=反向改名），不做完整操作栈。
- **F3 · 浏览形态**：时间线视图、地图式「按地点浏览」（复用已解析的 EXIF GPS）、幻灯片强化（过渡动效 / 间隔可调）。
- **F4 · 元数据写入**：把应用内的拍摄时间修正 / 标签真正写回文件 EXIF（当前只改应用内记录，需引入可写 EXIF 库，改动面较大）。
- **F5 · AI 增强**：批量分析选中项、按 AI 标签自动成组（AI 结果已持久化并纳入搜索）。
- **F6 · 实况照片合并**：把「图片 + 配对视频」视为一条记录（列表只显示图片、播放时可在静图与动态之间切换），避免配对视频在「视频」里重复出现。

---

## 四、X · 明确延后（非必要，不主动启动）

> 个人本地应用，以下项收益低或成本高，除非日后确有需要，否则不做。

| 编号 | 事项 | 暂缓理由 |
| --- | --- | --- |
| X1 | API Key 用 `safeStorage` 加密 + 主进程代理 | 个人自用、本地单机，泄露风险可接受 |
| X2 | 移除 `no-sandbox`、开启 sandbox、CSP、IPC 路径校验 | 同上，安全加固对个人使用收益低，改动面大 |
| X3 | 基础图片编辑（裁剪 / 翻转 / 调色） | 系统「预览 / 照片」已够用，重复造轮子 |
| X4 | 地图视图（EXIF GPS 展示） | 使用频次低（若要，先并入 F3 做轻量版） |
| X5 | 跨平台打包（Win/Linux）+ 自动更新 | 只在 macOS 自用 |
| X7 | `App.tsx` 大拆分 + 开启 `strict` + 引入 `@types/react` | 纯工程收益，风险高于当前收益 |
| X8 | 测试 / ESLint / CI | 个人项目，靠手动验证 + `tsc` + `build` 已足够 |
| X9 | 十万级库 Worker 化压测与调优 | 除非库规模真的到十万级 |
| X10 | 云同步 / Google Photos 等外部集成 | 与「纯本地」定位冲突 |
| X11 | 视频重编码导出 / 转码预览 | 依赖 ffmpeg，体积与复杂度陡增；原文件可直接复制 |

---

## 五、里程碑

| 里程碑 | 范围 | 关键交付 |
| --- | --- | --- |
| **v0.4 顺手版** | N1–N7（N1 / N2 / N3 / N4 / N6 已完成） | 待办：稳定性加固（N5）、封面落地（N7）；已完成：状态持久化、重命名不覆盖、搜索与筛选增强、体验细节打磨、视频体验完善，以及 **隐藏 / 智能相簿 / 用户标签 / 调整日期与时间** |
| **v0.5 增强版** | F1–F6 按需 | 轻量相册、撤销安全网、时间线 / 地图浏览、元数据写入、AI 增强、实况照片合并 |

> **推进方式**：按 N5 → N7 顺序做，每完成一项即验证「重启后是否仍顺手 / 是否会丢数据 / 视频是否别扭」，并回填本节状态。
