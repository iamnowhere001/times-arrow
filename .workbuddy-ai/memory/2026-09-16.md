# 2026-09-16

## P1 修复实施（10 项中完成 7 项，3 项并入 P2）

改动文件：`electron/main.js`、`electron/preload.js`、`electron/lib/csp.cjs`（新增）、
`vite.config.mts`、`package.json`、`src/App.tsx`、`src/types/global.d.ts`、
`src/lib/fs/pathUtils.ts`、`src/lib/media/photoGrouping.ts`、`src/utils/index.ts`、
`src/components/modal/RenameModal.tsx`、`src/components/modal/ExportModal.tsx`、
`src/components/modal/AiSettingsModal.tsx`、`src/components/detail/DetailsPane.tsx`、
`src/components/grid/ThumbnailImage.tsx`、`src/lib/cache/thumbCache.ts`、
`src/lib/fs/fileOperations.ts`、`src/lib/persistence/{persistence,aiCache}.ts`、
`src/services/aiService.ts`、`tests/**`（新增）、`eslint.config.mjs`、`.prettierrc.json`。

### 已完成

- **P1-6 CSP**：策略唯一定义在 `electron/lib/csp.cjs`，主进程 `onHeadersReceived` 与
  Vite 构建期注入的 `<meta>` 共用同一份字符串。生产 `default-src 'none'` 起手、
  `script-src` 不给 `unsafe-inline`；开发放行内联脚本 + HMR ws（否则 React Refresh 白屏）。
  meta 版本剔除 `frame-ancestors`（规范要求在 meta 中忽略，留着只会多一条告警）。
  vite host `0.0.0.0` → `127.0.0.1`，`electron:dev` 的 URL 同步改。
- **P1-7 原子占位**：`buildUniquePath` → `reserveUniquePath`（`open(path,'wx')`）。
  三处调用方一起改 —— 只换探测函数消不掉竞态，落盘原语也得原子。
  跨卷移动因此改用普通 `copyFile`（占位文件已存在，`COPYFILE_EXCL` 必然失败）。
- **P1-8 统一 IPC 协议**：30 个 handler → `{ok,data}` / `{ok,error,code}`；
  新增 `handle()` 包裹 `ipcMain.handle`（异常兜底 + 通道日志 + 重复注册检测），
  静态断言守着「不允许直接调用 ipcMain.handle」。删掉 20 余处重复 try/catch。
  **`global.d.ts` 重写成 `IpcResult<T>` 后 tsc 直接给出 123 处破坏点**，这是本次的安全网。
- **P1-9/10 文件名与路径**：`sanitizeFilename` 加固 + 新增 `validateFilename`，
  并建立不变式 **`validateFilename(name).ok` ⟹ `sanitizeFilename(name) === name`**；
  `RenameModal` 删掉自带的正则与保留名集合改为复用。`joinPath` 重写为对齐 `node:path.join`。

### P1-11 ESLint（最终落地，但 TS 侧降级）

**TypeScript 7 与 typescript-eslint 不兼容**：其 peer 范围是 `typescript >=4.8.4 <6.1.0`，
ESLint 启动时直接打印 `typescript-eslint does not support TS 7.0`，解析器加载不了。
因此 `eslint.config.mjs` 做成**能力探测**：装得上就开类型感知规则，装不上退化为只查 JS/CJS。
降级后仍有价值 —— 主进程（安全边界所在）本来就是 JS/CJS。
`npm run lint` 当前 **0 problems**。修掉的真实问题：2 处未使用的 `catch (error)`、
1 处 `throw new Error` 丢掉原始错误（补 `{ cause }`）、6 条失效的 eslint-disable 指令。

**安装绕行方案（有效，记下来）**：eslint 依赖树约 100 个包，超过单次调用的 65 路径上限。
有效做法是**先分块预装它的 30 个直接依赖（每块 8 个），最后再装本体**；剩余小插件逐个装。
25 次无脑重试对 eslint 本体不收敛，分块才是关键。

Prettier 已配置但**未对既有文件执行格式化**：仓库从未格式化过，一次性 `--write` 会产生
数千行 diff 把安全改动淹没。新文件已格式化，全量格式化建议单独找时间做。

### P1-13/14/15 并入 P2

与 P2-20/21/29/30/32 动同一批文件（App.tsx 拆分、hook 抽取、Modal 基座），分两批等于翻两遍。

### P2 进度

- **P2-17 dHash 收敛完成**：算法本体移到 `electron/lib/dhash.cjs`，主进程 `require`、
  渲染层 `import`（Vite 能直接消费本地 `.cjs`，已实测产物里内联了 `var n=9,r=8,i=.299,...`）。
  配套 `electron/lib/dhash.d.cts` 类型声明 + `tests/unit/dhash.test.ts` 13 项。
- **P2-18 照片时间语义收敛完成**：新增 `src/lib/media/photoTime.ts` 作为唯一入口
  （`photoTakenTime` 2 级链 / `photoOriginalTime` 3 级链 / `calendarDayKey` / `hasUsableTime`），
  12 处内联回退链全部收敛，`AdjustDateModal` 里那份抄来的 `baseTimeOf` 已删。
  `tests/unit/photoTime.test.ts` 18 项，含**扫描 src 全部 .ts/.tsx 的静态断言**防止回退链重新内联。

  **重要纠正**：原报告把 `getDateGroupKey`（按日）与 `groupByTime`（按年→月）列为「两套实现」
  是**不准确的** —— 粒度不同，服务不同视图，不应合并。真正的问题是时间回退链散落 12 处。

  **测试中发现的新限制（已写入报告附录 C.3，未擅自改行为）**：
  回退链 `dateTaken || dateCreated || lastModified` **无法区分共享 EXIF 拍摄时间的拷贝**——
  链式求值第一步就返回了，走不到 `dateCreated`。而重复检测里「拷贝继承 EXIF」是最典型场景。
  所以重复检测用逐字段比较的 `compareByOriginalTime`。
  由此 `LocationMap` 用 `photoOriginalTime` 做减法排序在「同组共享 EXIF」时全是并列，
  与重复检测的判断可能不一致 —— 建议后续统一，属行为变更。

- 其余 P2 项待做，任务清单已建（#18–#23）。

### P2 又完成三项（19 / 22 / 24）

- **P2-22 `placeIndex` 整表清空改真 LRU**：原是普通 Map + 满了 `clear()`，
  地图标签每帧都在查，一次清空表现为拖动时周期性卡顿。改用 `createLruCache` 后
  既只淘汰最久未用，也自动纳入内存压力释放范围。
  （`dateKeyCache.clear()` 保留 —— 那是跨天失效，与容量淘汰是两件事。）
- **P2-19 搜索索引缓存**：`matchesSearch` 原先每张照片每次按键都重做 `toLowerCase`。
  改为 `WeakMap<Photo, {fingerprint, haystack}>`（照片更新走整体替换，旧对象自动回收）。
  **坑**：相机标识必须**整体**入索引，拆成厂商/机型两段会让 `canon eos` 这类
  跨字段关键词匹配不到。已写进测试。
- **P2-24 存储损坏通知**：**先更正了原报告的误判** —— 经 `git show HEAD:electron/main.js`
  核对，备份逻辑（rename 到 `*.corrupt-<ts>`）在审查时就已存在，原报告说「没有备份」是错的。
  真正缺的是**告知用户**：新增 `storageNotices` 队列 + `storage-notices` IPC（取走即清空）
  + 启动时 Toast「哪个文件损坏、备份在哪」。只有改名真的成功才入队（首次运行不误报）。
  新增 `tests/main-process/storage-recovery.test.cjs` 18 项。

  **测试脚手架的坑（重要）**：
  1. `app.on('ready')` 会**预读** config.json / ai-config.json 进 storeCache，
     所以损坏文件必须在 `require(main.js)` **之前**放好 —— 为此给 harness 加了
     `beforeRequire(userData)` 钩子，并把该测试**单独成一个文件**。
  2. `ready` 里的 `Promise.all([readStore(...)])` 是 fire-and-forget，
     断言前要等一个宏任务，否则改名还没落地。
  3. **同一进程不能二次 `loadMainProcess()`** —— Node 缓存已 require 的模块，
     第二次拿不到新 handler。

### P2 又完成一项：16 拆 `utils/index.ts`

1024 行 → **86 行纯 barrel + 9 个聚焦模块**：
`duplicate/duplicateDetection`(486) / `fs/pathUtils`(222) / `media/mediaTypes`(202) /
`media/filenameRepair`(198) / `media/photoHash`(162) / `media/photoTime`(106) /
`format/format`(61) / `concurrency`(42) / `fs/fileToBase64`(27)。

**关键决策：保留 barrel 再导出，既有 `from '@/utils'` 一行不改。** 否则要同时改 20+ 个文件的
import，漏一个就是编译错误刷屏，反而看不清真正的行为变化。

**踩到的循环依赖风险**：`mediaTypes.ts` 原先从 `@/utils` 取 `extOfName`/`isVideoPhoto`；
`@/utils` 一旦变成再导出 `mediaTypes` 的 barrel 就成环。已让它只依赖 `pathUtils`。

**新增守卫** `tests/unit/utilsBarrel.test.ts` 40 项：34 个公开符号一个不能少 +
barrel 里不许有实现代码（按 `;` 切语句，只允许 import/export）+ 文件 < 120 行。
（逐行判断会误伤多行 `export {}`，必须按语句切。）

**静态断言的价值再次体现**：`dhash.test.ts` 里「渲染层必须从共享模块引入 dHash」那条
指向 `src/utils/index.ts`，拆分后立刻失败；已改指新位置并加强为「整个 src 只允许一处引用」。

### 冒烟测试可诊断性改进

原先环境不满足时抛看不懂的 `TypeError: ... reading 'on'`。现在：
`require('electron')` 拿不到 `app` → 打印可操作排查步骤并以 2 退出；
窗口始终没加载 → 以 3 退出并提示沙箱逃生口（**这条重要：页面没跑起来时「CSP 违规 0」是假阴性**）。

新增 `npm run test:smoke:no-sandbox`。**本机不带逃生口时 Electron 会 `FATAL: GPU process
isn't usable` 直接崩溃（退出码 133）**，所以本环境只能用这个脚本跑冒烟。
默认 `test:smoke` 保持不带逃生口，以免在正常机器上白白跳过沙箱路径。

### 本轮验证总览

| 项 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run lint` | 0 problems |
| `npm run test:unit` | 253 项通过（9 个文件） |
| `npm run test:main` | 88 项通过（3 个文件） |
| `npm run build` | 通过 |
| `npm run test:smoke:no-sandbox` | CSP 0 违规、0 控制台错误、`#root` 正常 |

### 补充环境经验

- 冒烟测试偶发失败（`require('electron')` 拿不到 API），**重跑即正常** —— 是那个间歇性的
  环境注入问题，不是代码问题。看到 Node 的模块加载栈就重跑一次。


### 验证体系（新增，已进 npm scripts）

- `npm run test:unit` —— vitest，**174 项**，6 个模块
  （filters / mapMath / placeIndex / photoGrouping / pathUtils / repairFileName+clusterBySize）。
- `npm run test:main` —— 打桩 electron 加载真实 main.js，**69 项**。
- `npm run test:smoke` —— 真启动 Electron，校验 CSP 未误伤、`#root` 有产出、preload 可用。
- 配置：`vitest.config.mts`（只收 `tests/unit/**`，alias `@`）。

### 本阶段抓到的真实缺陷（重要）

1. **P0 引入的路径授权 bug（高）**：`canonicalize` 对**尚不存在**的路径退化为
   `path.resolve`，拿到未解析符号链接的形式；授权根存的却是 realpath 形式。
   macOS 上 `/var` → `/private/var`，于是**临时目录下的合法重命名被整体判为未授权**。
   P0 的测试只覆盖了负向用例（拒绝未授权），正向路径漏掉了 —— 写测试时才发现。
   修法：向上找**最深已存在祖先**做 realpath，再把剩余的不存在部分拼回去。
2. `sortPhotosByTimeline` 注释说「缺失时间戳排到末尾」，实现 `|| 0` 实际排到**最前**。
3. `clusterBySize` 注释说「±10%」，实际是**以簇内最小体积为锚点**，区间 `[anchor, anchor×1.1]` 不对称。
4. `repairFileName` 会把扩展名转小写（`photo.JPG` → `photo.jpg`）。

后三条都**没有擅自改行为**，而是在测试里把实际语义钉住并注明与注释不符 —— 需产品决定。

### 环境经验（新增）

- **`npm install` 的 65 路径上限**：单次 npm install 新增/移动的路径超过约 65 个就会被
  `CODEBUDDY_BROKER_DENY` 拒绝（`Brokered host mkdir requires an available runtime file rule`）。
  `dangerouslyDisableSandbox` **无效** —— 拦的是 WorkBuddy 的文件系统代理层，不是 Chromium 沙箱。
  对策：清孤儿包 + 循环重试（npm reify 非原子，重试会逐步推进）；实在不行就分批装。
- **`npm run` 会污染子进程环境**：把 `npm run build` 和 `electron ./x.cjs` 串在同一条
  shell 命令里，Electron 会以 node 模式启动，`require('electron')` 拿不到 API。
  **必须分成两次 Bash 调用**。
- 用 `grep` 跑 `\|` 交替在本机 BSD grep 上常返回空 → 用 Grep 工具。
- 断言 `indexOf` 顺序时要锚定**调用点**：`function foo() {` 里也含 `foo()`，
  注释里也可能出现同样的字符串（本次踩到两次）。

---

## 文档同步：README / TODO / CODE_REVIEW 三份对齐实际状态

用户要求三份文档反映真实进度。先做了事实核对（跑 typecheck / lint / test / build 取实测数），
再动笔，避免写进推测值。

### 实测基线（写进文档的数字都出自这里）

| 项 | 结果 |
|---|---|
| `npm run typecheck` | 通过，零错误 |
| `npm run lint` | 0 problems（TS 侧降级：typescript-eslint 不支持 TS 7） |
| `npm run test:unit` | **266 项** / 10 文件（此前记录是 174，已长） |
| `npm run test:main` | **88 项** / 3 文件（22 + 18 + 48；此前记录 69） |
| `npm run build` | 主 bundle 663.05 kB（gzip 184.41）+ places chunk 58.80 kB（gzip 24.42）+ CSS 101.26 kB |
| 规模 | `App.tsx` 1279 行；`src/hooks/` 18 个 / 4287 行（新增 15 个 / 3856 行）；`src/` 30 tsx + 49 ts ≈ 23249 行；`electron/main.js` 2746 行 |

### 关键发现：工作区有一批**未提交**的 P2 成果

`git status` 里 15 个新 hook + `ToastStack.tsx` + `ViewErrorFallback.tsx` 全是 untracked。
也就是说「App.tsx 拆分」「分层 ErrorBoundary」已经做完，但**还没进版本库** ——
写文档时必须按磁盘实际状态写，并在 CODE_REVIEW 里如实标注。
另外 `places.data.json` 已落地（places.ts 1767 → 96 行），即 P2-27 也已完成。

P2 计数口径：17 项（16–32）+ 并入的 P1-13/14/15 = **20 项，已完成 9，剩余 11**。
已完成：13（App hooks）/ 16（拆 utils）/ 17（dHash）/ 18（时间语义）/ 19（搜索索引）/
22（LRU）/ 24（存储通知）/ 27（places 外置）/ 30（分层 ErrorBoundary）。

### 文档结构调整（便于后续维护）

- **CODE_REVIEW.md 新增 §0.1「发现项修复状态总览」**：把 40+ 个编号（S/C/I/R/A/U/K/P/F/M/G/N/B）
  逐条列出状态。正文**不回改**（保留审查当时的判断以便追溯），状态统一查这一张表 ——
  比逐条 inline 标记更省事，也不会出现「有的标了有的没标」的不一致。
- 新增**附录 D**（P2 后半批：App 拆分 / places 外置 / 分层 ErrorBoundary / ToastStack +
  遗留清单 + 新发现），附录 C 只保留收敛类。
- §15 结语加了「后记」，点明本报告**三处判断在动手时被证伪**（P1 配置备份、N11/D2 日期分组、
  P0 授权 bug），提醒下次审查这些是踩过的地方。
- **TODO.md**：X2 从「延后」改为 ✅ 已完成（当初「改动面大」的预估不成立）；X7 / X8 标 🔶 部分完成；
  新增 K31（原图判定两套逻辑不一致）、K32（注释与实现不符待产品决定）、F17（全量 Prettier 格式化）；
  加了状态图例行到约定表。
- **README.md**：新增「安全边界」「代码组织约定」两节 + 开发与校验命令表；
  目录结构按实际重写（含 `electron/lib/*`、`src/hooks/`、`tests/`）；
  已知限制拆成「功能缺口」与「实现约束」两组，删掉已过时的「安全配置偏松」。

### 经验

- **写「进度类」文档前先跑一遍工具取数**。文档里原有的 174 / 69 项测试数已经过时，
  若不实测就会把旧数字当成现状写进新的总结行。
- **文档说「已完成」前先看 `git status`**：本次所有 P2 成果都还是 untracked，
  只按 git log 判断会漏掉整批工作。

### 复审修正（同轮补充）

写完三份文档后又做了一轮事实核对，改掉 4 处：

1. **ErrorBoundary 实际是 5 处，不是 4 处**：四个整页视图（图库/时光画廊/按地点浏览/重复检测）
   共用 `ViewErrorFallback`，**详情面板**另有一层**内联降级**（保留 320px 侧栏宽度的一句提示，
   不用整页接管 —— 面板是附属区域，让它顶掉主内容区不合理）。三份文档已同步改成精确表述。
2. **TODO 批次 B/C 的文件指针已过期**：K7/K9/K10/K11/K20/K26 还指向 `src/App.tsx` 与
   `src/utils/index.ts`，但这两个文件本次都拆了。已按实际刷新：
   AI 缓存写入点 → `usePersistedLibraryData.ts:224`（`aiCacheRef.current.set(target.path, …)`）；
   并查集/体积预筛 → `lib/duplicate/duplicateDetection.ts`；重命名预览 → `hooks/useFileOperations.ts`；
   相簿 → `hooks/useSmartAlbums.ts`。
3. **K4 优先级上调**：主进程缺 `uncaughtException` 兜底意味着触发就是整个应用静默退出，
   比「只是白屏」重，已从「低」改为「中低」。
4. **CODE_REVIEW §0.1 加了命名空间提醒**：§6 的 `K1`/`K2`（缓存层）与 TODO 的 K 批次是两套编号，
   不能混用。

### 复用经验

- 文档里写「N 处」「N 个」这种计数前，grep 出来**逐条数一遍**再落笔 —— 本次靠
  `sed -n '925,1000p'` 才发现第 5 处 ErrorBoundary，只按记忆写就是错的。
- 大重构之后，文档里的**文件路径**会成批失效。凡是提到具体文件的条目，
  重构完要顺手刷一轮（本次 TODO 有 6 条需要改）。

### 提交（2026-09-16 12:55）

用户确认后提交，按「一次提交只做一件事」拆成 4 个（未 push，等用户确认）：

1. `27662b5 refactor(hooks)` 拆分 App.tsx 为 15 个功能域 hook（+4321 / -2888，18 文件）
2. `5203e51 refactor(build)` vite 配置补 CSP meta 注入 + 共享 CJS→ESM 插件
3. `7b74caa docs` 三份文档同步（+488 / -102）
4. `4c29e9b chore` 项目记忆

提交前检查：`.env.local`（`*.local`）与 `default.profraw`（`*.profraw`）均已被 .gitignore 覆盖，
新文件扫描无密钥。提交后复跑 typecheck / lint / test 全绿（单测 266、主进程 88）。
