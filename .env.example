# PhotoMinder 环境变量示例
#
# 复制为 .env.local 后填入真实值（.env.local 不会被提交）：
#   cp .env.example .env.local
#
# DeepSeek API Key（可选）：用于「详情面板」中的 AI 图片描述与标签生成。
# 该变量仅由 Electron 主进程读取，不会注入渲染进程产物。
# 未配置时，其余全部功能均可正常使用，仅 AI 分析不可用。
DEEPSEEK_API_KEY=

# 以下为可选覆盖项（留空则使用默认值）
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-flash
